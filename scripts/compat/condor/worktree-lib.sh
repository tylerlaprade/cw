#!/usr/bin/env bash
# Placeholder for Condor's exact `scripts/test-cw.sh` passthrough.
# The legacy cw-shell suite copies this file into scratch workspaces, but the
# Rust compatibility wrapper does not source or depend on it.
